import { TablatureLinePipe } from './tablature-line.pipe';

describe('TablatureLinePipe', () => {
  it('create an instance', () => {
    const pipe = new TablatureLinePipe();
    expect(pipe).toBeTruthy();
  });
});
