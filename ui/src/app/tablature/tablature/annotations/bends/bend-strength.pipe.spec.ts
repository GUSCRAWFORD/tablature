import { BendStrengthPipe } from './bend-strength.pipe';

describe('BendStrengthPipe', () => {
  it('create an instance', () => {
    const pipe = new BendStrengthPipe();
    expect(pipe).toBeTruthy();
  });
});
